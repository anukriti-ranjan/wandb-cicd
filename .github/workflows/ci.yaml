name: GitHub Actions Demo

on: [push]

jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
          echo "hello 🎉 !! The job was automatically triggered by a ${{ github.event_name }} event."
